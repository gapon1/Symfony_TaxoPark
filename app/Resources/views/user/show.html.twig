{% extends 'base.html.twig' %}

{% block title %} Taxo Park Show {{ user.name }}{% endblock %}

{% block body %}

    <table class="table table-striped">
        <thead>
        <tr>
            <th>Name</th>
            <th>Password</th>
            <th>Email</th>
            <th>Id</th>
            <th>Role</th>
        </tr>
        </thead>

        <tbody>
        <tr>
            <td>{{ user.name }}</td>
            <td>{{ user.password }}</td>
            <td>{{ user.email }}</td>
            <td>{{ user.UseId }}</td>
            <td>{{ user.role }}</td>
        </tr>
        </tbody>

    </table>

    <br>
    {#<div class="text-center">#}
    {#<img width="500"  src="{{ asset('../img/') }}{{ car.carImg }}">#}
    {#</div>#}

{% endblock %}
